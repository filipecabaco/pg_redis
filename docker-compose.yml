services:
  postgres:
    build: .
    image: pg_redis:latest
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: postgres
    ports:
      - "5432:5432"
      - "6379:6379"
    volumes:
      - ./docker/postgres/init.sql:/docker-entrypoint-initdb.d/init.sql
    command: >
      postgres
        -c shared_preload_libraries=pg_redis
        -c redis.port=6379
        -c redis.listen_address=0.0.0.0
        -c redis.default_db=${PG_REDIS_DEFAULT_DB:-8}
        -c redis.workers=${PG_REDIS_WORKERS:-4}
        -c redis.batch_size=${PG_REDIS_BATCH_SIZE:-64}
        -c redis.storage_mode=${PG_REDIS_STORAGE_MODE:-auto}
        -c redis.password=testpass
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 2s
      timeout: 5s
      retries: 15

  e2e:
    build: ./docker/e2e
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      REDIS_HOST: postgres
      REDIS_PORT: "6379"
      REDIS_PASSWORD: testpass
      DATABASE_URL: postgres://postgres:postgres@postgres:5432/postgres
    network_mode: "service:postgres"
    profiles: ["e2e"]

  parity:
    build: ./docker/e2e
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      PG_REDIS_HOST: postgres
      PG_REDIS_PORT: "6379"
      PG_REDIS_PASSWORD: testpass
      REDIS_REF_HOST: redis
      REDIS_REF_PORT: "6379"
      REDIS_REF_PASSWORD: testpass
    command: ["bun", "run", "parity.ts"]
    profiles: ["parity"]

  clients:
    build: ./docker/clients
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      REDIS_HOST: postgres
      REDIS_PORT: "6379"
    network_mode: "service:postgres"
    profiles: ["clients"]

  redis:
    image: redis:7-alpine
    command: ["redis-server", "--requirepass", "testpass"]
    ports:
      - "6380:6379"
    healthcheck:
      test: ["CMD-SHELL", "redis-cli -a testpass ping | grep -q PONG"]
      interval: 2s
      timeout: 5s
      retries: 15
    profiles: ["bench", "parity"]
